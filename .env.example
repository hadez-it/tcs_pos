# Supabase Configuration
# Set these in Vercel Dashboard -> Settings -> Environment Variables
# If not set, the app falls back to a fully persistent LocalStorage mock database.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
